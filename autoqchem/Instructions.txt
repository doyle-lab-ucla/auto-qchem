# non-python, install openbabel binary package (we will install python bindings to that package later)
https://github.com/openbabel/openbabel/releases/download/openbabel-3-0-0/OpenBabel-3.0.0.exe

# install latest miniconda

# open miniconda prompt

# create a conda environment
conda create --name autoqchem python=3.7

# activate the environment
conda activate autoqchem

# install mainstream python packages using conda
conda install jupyter pandas scipy matplotlib pymongo pyyaml fabric

# chem packages using pypi (python package index), these are outdated/not-present in conda
pip install openbabel imolecule

